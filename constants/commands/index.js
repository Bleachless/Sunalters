const { togglePause } = require("../../constants/commands/pause.js");
const { toggleResume } = require("../../constants/commands/resume.js");
const { toggleSkip } = require("../../constants/commands/skip.js");
const { toggleStop } = require("../../constants/commands/stop.js");
const { toggleBack } = require("../../constants/commands/back.js");

module.exports = {
    togglePause,
    toggleResume,
    toggleSkip,
    toggleStop,
    toggleBack
};


