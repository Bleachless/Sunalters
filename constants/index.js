const { togglePause } = require("../constants/commands/index");
const { toggleResume } = require("../constants/commands/index");
const { toggleSkip } = require("../constants/commands/index");
const { toggleStop } = require("../constants/commands/index");
const { toggleBack } = require("../constants/commands/index");

module.exports = {
    togglePause,
    toggleResume,
    toggleSkip,
    toggleStop,
    toggleBack
};